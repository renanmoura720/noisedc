# Fixtures de teste

`exemplo_curto.wav` — 3 segundos de áudio sintético (tom de 120 Hz + ruído),
usado por testes que precisam de um arquivo real em disco em vez de um sinal
gerado em memória. Não é uma amostra do conjunto de dados da dissertação.
